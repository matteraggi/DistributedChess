# DistributedChess

to start another istance of backend (cd LobbyService):  dotnet run --urls="http://localhost:5001"

to start another istance of frontend:  ng serve --port 4201

deploy aggiornato sul docker: docker compose up -d --build --force-recreate